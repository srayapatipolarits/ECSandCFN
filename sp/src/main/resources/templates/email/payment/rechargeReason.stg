SurePeople Subscription Renewed
$if(isCreditCard)$
for account no: $account.accountNumber$
$else$
<br/>
Date: $ spPlans.billingCycleStartDate;format="MMM dd,yyyy" $ to $spPlans.nextPaymentDate;format="MMM dd,yyyy"$
$if(isBusiness)$
<br/>
$spPlans.billingCycle.billingCycleType $ Payment Plan
<br/>
Plan name : $spPlans.name$ : $spPlans.numMember$ $if(isPA)$ Portraits  $else$ Member Accounts $endif$ , $spPlans.numAdmin$ Admin Accounts <br/>
$endif$
$endif$
