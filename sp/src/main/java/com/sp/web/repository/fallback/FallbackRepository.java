package com.sp.web.repository.fallback;

import com.sp.web.model.fallback.FallbackBean;
import com.sp.web.repository.generic.GenericMongoRepository;

/**
 * FallbackRepository holds the interface for the fallback repository.
 * 
 * @author pradeepruhil
 *
 */
public interface FallbackRepository extends GenericMongoRepository<FallbackBean> {
}
